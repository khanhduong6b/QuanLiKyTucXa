@model IEnumerable<QuanLiKyTucXa.Models.PhieuDangKy>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TinhTrang)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MssvNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TinhTrang)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MssvNavigation.Mssv)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaHoSo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaHoSo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaHoSo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
